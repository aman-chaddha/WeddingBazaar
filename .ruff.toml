# Ruff configuration
# Docs: https://docs.astral.sh/ruff/configuration/

line-length = 100
indent-width = 4
target-version = "py310"

# Lint settings
# E,F: pycodestyle + pyflakes
# I: isort (import sorting)
# B: flake8-bugbear
# UP: pyupgrade
# C4: flake8-comprehensions
[lint]
select = [
    "E",
    "F",
    "I",
    "B",
    "UP",
    "C4",
]
# Ignore rules
# - F401: imported but unused (commonly in __init__.py for package exports)
# - E203/E266: whitespace/line-break rules clashing with common formatters
# - B008: FastAPI encourages Depends(...) in function signatures
ignore = [
    "F401",
    "E203",
    "E266",
    "B008",
]

# Exclude generated or external code
exclude = [
    ".git",
    "venv",
    "__pycache__",
    "client/",
    "server/alembic/versions/",
]

[format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
